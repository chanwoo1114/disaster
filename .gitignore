# Environment
.env
.venv/

# IDE — 공유 가능한 설정만 포함. 개인 상태(workspace.xml 등)는
# JetBrains 가 만든 .idea/.gitignore 가 걸러낸다

# Python
__pycache__/
*.pyc

# Node
node_modules/
dist/

# Data
backend/app/data/

# OS
.DS_Store

# 시뮬레이션 데이터 (대용량)
*.zip
# 압축을 푼 결과 폴더 (1.6GB) — tt.zip 만 LFS로 관리한다
/tt/

# tt.zip은 예외적으로 포함 (Git LFS)
!tt.zip
img.png
